services:
  metadata-cleaner:
    build: .
    image: metadata-cleaner:latest
    ports:
      - "8000:8000"
    environment:
      - BASE_URL=http://localhost:8000
      - TTL_HOURS=24
    volumes:
      - ./storage:/app/storage
